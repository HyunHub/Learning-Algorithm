#include <stdio.h>
#include <vector>
#include <algorithm>
using namespace std;

// 교집합(투포인터 알고리즘)
  
int main() {
	
	int n, m, i;

	scanf("%d", &n);
	
	int a[n];
	
	for(i=1;i<=n;i++) {
		scanf("%d", &a[i]);
	} 

	scanf("%d", &m);
	
	int b[m];
	
	for(i=1;i<=m;i++) {
		scanf("%d", &b[i]);
	} 
	
}
